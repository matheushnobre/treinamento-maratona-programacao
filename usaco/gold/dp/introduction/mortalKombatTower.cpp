#include <bits/stdc++.h>
using namespace std;

#define MAX 200001

int main(){
    int t, n;
    int vetor[MAX];
    int dp[MAX] = {INT_MAX};
    cin>>t;

    for(int ct=0; ct<t; ct++){
        cin>>n;
        for(int i=0; i<n; i++){
            cin>>vetor[i];
        }

        // TO DO: Criar DP para resolver o problema
        
    }
}