#include <bits/stdc++.h>
using namespace std;

int main(){
    // int n = 3;
    // int a[n] = {6, 4, 1};

    // // BubbleSort
    // for (int i = 0; i < n; i++) {
    //     for (int j = 0; j < n - 1; j++) {
    //         // Swap adjacent elements if they are in decreasing order
    //         if (a[j] > a[j + 1]) {
    //             swap(a[j], a[j + 1]);
    //         }
    //     }
    // }

    // // Counting Sort
    // #define MAX 1000
    // int n = 12, maior = 0;
    // int a[n] = {6, 4, 1, 4, 9, 3, 1, 4, 2, 6, 101, 2};
    // int counting[MAX] = {0};

    // for (int i=0; i<n; i++){
    //     counting[a[i]] += 1;
    //     if (a[i] > maior){
    //         maior = a[i];
    //     }
    // }

    // int index=0;
    // for (int i=0; i<=maior; i++){
    //     for (int j=0; j<counting[i]; j++){
    //         a[index] = i;
    //         index++;
    //     }
    // }

    // Métodos
    // int n = 10;
    // int a[n] = {6, 4, 1, 4, 9, 3, 1, 4, 2, 6};
    // sort(a, a+n);
    // for(int i=0; i<n; i++)
    //     cout<<a[i]<<" ";
    // cout<<endl;

    // vector<int> v = {4, 2, 5, 3, 5, 8, 3};
    // sort(v.begin(), v.end());
    // for(unsigned int i=0; i<v.size(); i++)
    //     cout<<v[i]<<" ";
    // cout<<endl;

    // sort(v.rbegin(), v.rend());
    // for(unsigned int i=0; i<v.size(); i++)
    //     cout<<v[i]<<" ";
    // cout<<endl;

    // string s = "MATHEUS";
    // sort(s.begin(), s.end());
    // cout<<s<<endl;

    return 0;
}